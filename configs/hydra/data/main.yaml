keypoint_dim: ${pred_keypoint_dim}
is_interactive: false
is_noisy: ${is_noisy}
noise_rate: mask
noise_keypoint:
persons_num: 1
use_mask: ${use_mask}

train_pose_format: ${train_pose_format}
pred_pose_format: ${pred_pose_format}

use_expmap: false
use_rotmat: true
use_euler: true
use_action: true

use_quaternion: false
is_testing: false
is_visualizing: false
batch_size: 16
shuffle: false
pin_memory: false
num_workers: 0
overfit:
normalize: ${normalize}
metadata_path:
