defaults:
  - _self_
  - data: main
  - model:

dataset: ???
dataset_name: ???
ground_truth: false
device: cuda
keypoint_dim: ???
use_mask: false
model:
  keypoint_dim: ${keypint_dim}
  use_mask: ${use_mask}
data:
  keypint_dim: ${keypoint_dim}
  use_mask: ${use_mask}
pred_frames_num:
load_path:
index:
images_dir:
save_dir: